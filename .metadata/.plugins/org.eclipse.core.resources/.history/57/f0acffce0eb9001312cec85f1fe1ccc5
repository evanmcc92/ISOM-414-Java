import javax.swing.*;

import java.awt.*;

public class taxcalculator2 extends JApplet{
	public void paint(Graphics g){

		
		setSize(500,500);
		
		
		String empname, empid, output;
		int sales, statetax, commission, netincome;
		int counter1 = 1;
		int x = 1;
		int y = 25;
		int z = 45;
		int client, var1, var2, var3;
		
		
		client = Integer.parseInt(JOptionPane.showInputDialog("Enter Number of Clients"));

		while(counter1 <= client){

			empid = JOptionPane.showInputDialog("Enter Employee ID");
			empname = JOptionPane.showInputDialog("Enter Employee Name");
			sales = Integer.parseInt(JOptionPane.showInputDialog("Enter Monthly Sales"));
			statetax = Integer.parseInt(JOptionPane.showInputDialog("Enter State Tax Rate"));
			
			if(){
				var1 = 1;
			}
			
			switch (commission){
			case 10000:
				commission = .10;
				break;
			case 10000 through 5000.01:
				commission = .075;
				break;
			case sales >= 1:
				commission = .05;
				break;
			default:
				commission = 0;
			}
			
			
			netincome = 2000+(sales*commission);
			netincome = netincome-(statetax * netincome);
			
			
			output = "ID\tEmployee Name\tTotal Sale\tCommission\tState Tax\tNet Income\n"
					+ empid+"\t" +empname+"\t"+sales+"\t"+commission+"\t"+statetax+"\t"+netincome;
			
			
			JOptionPane.showMessageDialog(null, output);
			
			g.drawString("ID\tEmployee Name\tTotal Sale\tCommission\tState Tax\tNet Income", 25,y);
			g.drawString(empid+"\t" +empname+"\t"+sales+"\t"+commission+"\t"+statetax+"\t"+netincome, 25,z);
			
			counter1++;
			y = y + 45;
			z = z + 45;
		}
	}
}